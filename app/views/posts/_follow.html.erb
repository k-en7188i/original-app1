<% if current_user != post.user %>  
  <% if post.user.followed_by?(current_user) %>
    <%= link_to image_tag("following01.png", size: "45x45"), user_relationships_path(post.user), class: "star-icon", method: :delete %>
  <% else %>
    <%= link_to image_tag("notFollowing01.png", size: "45x45"), user_relationships_path(post.user), class: "star-icon", method: :post %>
  <% end %>
<% end %>