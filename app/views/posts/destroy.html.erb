<%= render "shared/second-header"%>

<div class="contents row">
  <div class="success">
    <h3>削除が完了しました。</h3>
    <a class="btn" href="/">投稿一覧へ戻る</a>
    <% <a class="btn" href="/users/<%= current_user.id %>">マイページへ戻る</a> %>
     <%#= link_to "マイページへ戻る", user_path(current_user), class: "btn" %>
  </div>
</div>