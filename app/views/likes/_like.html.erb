<% if user_signed_in? && post.liked_by?(current_user)%>
  <%=link_to "いいね済", post_likes_path(post.id),  method: :delete, class: "star-icon", remote: true %>
<% else %>
  <%=link_to "いいねする", post_likes_path(post.id), method: :post, class: "star-icon", remote: true %>
<% end %>